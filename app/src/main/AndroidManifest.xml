<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAD_Practicals"
        tools:targetApi="31">
        <activity android:name=".DisplayStudNameAndMarks" />
        <activity android:name=".AcceptUserPass" />
        <activity android:name=".AcceptStudInfo" />
        <activity android:name=".BluetoothOnOff" />
        <activity android:name=".SimpleCalculator" />
        <activity android:name=".StudRegister" />
        <activity android:name=".ToastMessage" />
        <activity android:name=".DateTimePicker" />
        <activity android:name=".HelloWorld" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".ListSensors" />
        <activity android:name=".PlayAudioFile" />
        <activity android:name=".ImageTranslations" />
        <activity android:name=".SQLiteDB" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>